{
  "name": "hakenbox-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:vite": "pnpm --filter playground-vite dev",
    "dev:next": "pnpm --filter playground-next dev",
    "test": "pnpm --filter hakenbox test",
    "test:watch": "pnpm --filter hakenbox test:watch",
    "copy-readme": "cp ./README.md ./packages/hakenbox/README.md",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
